apiVersion: v1
kind: Service
metadata:
  name: events-worker
  namespace: go-app
spec:
  type: ClusterIP
  selector:
    app: events-worker
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics